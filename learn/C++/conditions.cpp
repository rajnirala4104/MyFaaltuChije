#include <iostream>
using namespace std;

int main()
{

      // cout << "Enter you age: ";

      // int age;
      // cin >> age;

      // if (age < 18)
      // {
      //       cout << "Oops!! sorry You can't drive the car " << endl;
      // }
      // else
      // {
      //       cout << "You can drive the car" << endl;
      // }

      // --------------- problem for practice -------
      // which one is greates number

      // int a, b, c;
      // cin >> a >> b >> c;
      // if (a < b)
      // {
      //       if (b < c)
      //       {
      //             cout << "C is the greates number you've entered " << c << endl;
      //       }
      //       else
      //       {
      //             cout << "B is the greates number you've entered " << b << endl;
      //       }
      // }
      // else
      // {
      //       if (a < c)
      //       {
      //             cout << "C is the greates number you've entered " << c << endl;
      //       }
      //       else
      //       {
      //             cout << "A is the greates number you've entered " << a << endl;
      //       }
      // }

      // ------ second problem ----------
      // even and odd

      cout << "Enter a number" << endl;

      int num;
      cin >> num;

      if (num % 2 == 0)
      {
            cout << "You've entered EVEN" << endl;
      }
      else
      {
            cout << "You've entered ODD" << endl;
      }
      return 0;
}